apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: voice-services
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/FlunkyMonkey/k8s-lab.git
    targetRevision: HEAD
    path: apps/voice-services
  destination:
    server: https://kubernetes.default.svc
    namespace: voice-services
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
